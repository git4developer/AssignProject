package com.task.demo.helper;

import java.time.LocalDateTime;
import java.time.format.DateTimeFormatter;

public class GetDayMonthYearDiff {
	
	
	
//	 public List<YourEntity> findBetweenIsoDates(Date startDate, Date endDate) {
//	        MatchOperation matchOperation = Aggregation.match(
//	            Criteria.where("iso_date").gte(startDate).lte(endDate)
//	        );
//
//	        Aggregation aggregation = Aggregation.newAggregation(matchOperation);
//
//	        return mongoTemplate.aggregate(aggregation, "your_collection", YourEntity.class)
//	                                  .getMappedResults();
//	
	
	
	
	
	
	
	
	
	
//	public static LocalDateTime getDiiff(String iso) {
//		 String isoDateTime = iso;
//		 return isoDateTime;
//
////	        // Define the date-time format
////	        DateTimeFormatter formatter = DateTimeFormatter.ISO_LOCAL_DATE_TIME;
////
////	        // Parse the ISO date-time string into a LocalDateTime object
////	        LocalDateTime dateTime = LocalDateTime.parse(isoDateTime, formatter);
////
////	        // Extract year, month, day, and time
//////	        int year = dateTime.getYear();
//////	        int month = dateTime.getMonthValue();
//////	        int day = dateTime.getDayOfMonth();
//////	        int hour = dateTime.getHour();
//////	        int minute = dateTime.getMinute();
//////	        int second = dateTime.getSecond();
////	        
////	        //days difference
////	        // LocalDate date1 = LocalDate.parse(isoDate1, formatter);
////	      //  LocalDate date2 = LocalDate.parse(isoDate2, formatter);
////	      //  long differenceInDays = ChronoUnit.DAYS.between(date2, date1);
////	        
////	        
////	        //hour difference
//////	        LocalTime time1 = LocalTime.parse(isoTime1, formatter);
//////	        LocalTime time2 = LocalTime.parse(isoTime2, formatter);
//////
//////	        // Calculate the difference in hours
//////	        long differenceInHours = ChronoUnit.HOURS.between(time2, time1);
//	        
//	        
//	 //       return dateTime;
//     
//}

    

}
